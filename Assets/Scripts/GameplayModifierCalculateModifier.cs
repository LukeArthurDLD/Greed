using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameplayModifierCalculateModifier : MonoBehaviour
{
    [Header("Child Script")][SerializeField]
    internal GameplayModifierSystem systemManager;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
